"""Experimental Markov certification components.

This package is the new verifier path being built alongside the legacy
`sysml_deps.py` + `reconstruct_closure.py` closure checker. The proof-artifact
writer/checker is available as an optional training output and as standalone
certification CLIs.
"""
