numpy>=1.24
antlr4-python3-runtime==4.13.2
flask>=3.0

# Optional for Torch reference components under rl/ and runtime-verification/.
torch>=2.0

# nuXmv 2.1.0 (binary included in artifact for Linux x86_64)
# Download from https://nuxmv.fbk.eu if needed for other platforms
